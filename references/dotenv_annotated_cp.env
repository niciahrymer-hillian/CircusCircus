# CircusCircus .env reference copy
# [WHY] Centralizes DB credentials for local and Docker/PostgreSQL use
# [SECURITY] Never commit real passwords to version control
# [EFFECT] Used by both Flask app and Docker Compose for DB connection
DATABASE_URL=postgresql://ccuser:Elephant@db/circuscircus
